# GuDuu OS — 开发日志 (Dev Log)

## 2026-08-06 — GuDuu OS 1.26.1 (patch)

- 优化：OEM bot/web 多架构镜像同步发布到公开 Docker Hub，并继续保留平台自建仓
  与 GHCR；三个仓库提供相同不可变摘要及 `vX.Y.Z`、`X.Y.Z` 双版本 Tag。
- 变更：一键安装与宿主更新器默认优先从 Docker Hub 按摘要拉取，方便国内服务器使用
  标准 Docker 加速器；失败后整组回退平台自建仓，最后回退 GHCR。
- 安全：Nexus 仍只接受 CI 签名的 GHCR 摘要作为事实源，Docker Hub 与自建仓必须匹配
  同一 digest；OEM 节点不保存仓库账号、Token，也不依赖可移动 `latest`。
- 边界：无需数据库迁移；新镜像只自动进入节点 #2 灰度候选，节点 #1 与 #3 不自动更新。

## 2026-08-06 — GuDuu OS 1.26.0 (minor)

- 新增：macOS 与 Windows 桌面端支持工作区邀请深链
  `guduu://join/<Matrix Space ID>`，可在冷启动和 App 已运行时打开共享加入流程。
- 安全：main 进程只白名单放行工作区邀请，拒绝其他协议、主机、查询参数、
  hash、路径穿越和畸形 Space ID；preload 不暴露任意导航或 IPC 能力。
- 优化：深链使用 renderer 就绪握手与 main 内存队列，避免冷启动、页面重载或
  登录页状态丢失邀请；未登录用户登录后继续原有 `/join/:space` 流程。
- 边界：本次仅完成本地开发与非生产包装验证，未开放公开下载、自动更新或
  任何应用商城提交。

## 2026-08-05 — GuDuu OS 1.25.0 (minor)

- 新增：macOS 与 Windows 桌面端在应用处于后台时，可将现有的新私信提醒
  补充为操作系统通知；点击通知会恢复并聚焦 GuDuu OS 主窗口。
- 安全：通知只通过逐项命名的 preload API 传递受限纯文本，main 进程校验
  sender、顶层 frame、字段和长度，禁止 renderer 指定本地路径、URL 或任意选项。
- 优化：前台窗口继续只显示页内 toast，系统通知在 main 进程限流并限制
  同时存活数量，避免重复提醒或被异常消息刷屏。
- 边界：本次仅完成本地开发、测试和未签名包装验证；应用完全退出后的离线
  推送仍属于后续 APNs/FCM/Matrix push gateway 模块，本次未启用。

## 2026-08-05 — GuDuu OS 1.24.2 (patch)

- 修复：macOS 与 Windows 桌面端不再把 Matrix access token、多账号会话和 device ID
  明文长期保存在 `localStorage`，改由 Electron main 进程使用系统安全存储加密落盘。
- 安全：凭据 IPC 校验主窗口 sender、顶层 frame、参数结构和大小；renderer 只获得
  逐项命名的读写/清除方法，系统钥匙串不可用时禁止降级明文。
- 优化：旧桌面会话会在加密写入成功后自动清除明文，Web 继续通过同一会话适配层保持
  原有登录、多账号切换和退出行为；路由放行以异步安全仓库为准。
- 边界：本次只开发、测试和生成本地未签名包，不开放桌面下载、不启用自动更新，
  不创建或提交任何应用商城条目。

## 2026-08-05 — GuDuu OS 1.24.1 (patch)

- 修复：公开 OEM 安装器在受支持的干净 Ubuntu 22.04+ / Debian 12+ 上自动准备
  基础工具、Docker Engine 与 Compose 插件，不再要求客户先手动安装 Docker。
- 新增：为存量 OEM 节点提供严格 Git tag 的一次性宿主更新工具迁移；迁移持有升级锁、
  备份旧代理与 systemd 单元，只更新宿主工具并默认关闭客户节点自动安装，不重建或
  切换 bot/web 容器，也不覆盖 ``.env``、数据库、证书及反向代理定制。
- 优化：Web 多架构镜像只在 GitHub Runner 原生架构构建一次与 CPU 无关的 Vue 产物，
  再装入 amd64/arm64 Caddy 运行时，避免 arm64 QEMU 重复执行 npm 构建导致任务长时间卡住。
- 文档：部署中心明确安装命令已自动填入审批域名、DNS 可在安装后解析，以及解析生效后
  Caddy 会自动签发 HTTPS 证书；品牌、邮箱、主 AI 与支付统一在网页首次向导配置。
- 修复：公司灰度节点清空后重新一键安装时，优先取得已明确分配给自己的
  候选镜像摘要，不再悄悄退回旧正式版；未分配的普通 OEM 仍只能安装正式版。
- 运营：Nexus OEM 注册、验证码登录与找回密码已完成真实验收；OEM 支付渠道
  按负责人决定暂停接入，不阻断当前镜像与一键安装验收。

## 2026-08-05 — GuDuu OS 1.24.0 (minor)

- 新增：OEM 节点安装完成后，初始管理员首次进入 OS 必须依次配置品牌名称与 Logo、
  发信邮箱、主 AI 提供方/API 和支付渠道；完成后可从管理后台“系统设置”重复修改。
- 安全：SMTP 密码、模型 API Key 和支付密钥使用节点独立主密钥加密保存在 cosmac
  PostgreSQL，公开品牌接口和管理员接口都不返回密钥原文，空白提交保留旧密钥。
- 变更：节点升级默认改为“发布更新通知 → 节点管理员确认 → 宿主代理主动拉取”；
  普通 OEM 不再因收到任务自动安装，内部灰度节点可显式开启自动候选安装。
- 发行：GHCR 与自建仓统一提供 `vX.Y.Z`、`X.Y.Z` 两个标准 Tag，自动更新仍冻结
  `@sha256:digest`；自建仓改为匿名只读，公网写方法由 Caddy 拒绝。
- 边界：节点支付适配器尚未完成真实下单、回调验签和沙箱联调，向导只允许安全预存
  配置并明确显示“待接入”，不会因为保存凭据就开放收款。

## 2026-08-05 — GuDuu OS 1.23.0 (minor)

- 新增：macOS 与 Windows 桌面端首个可运行版本，共享现有 Vue/TypeScript
  页面与业务能力，不复制维护另一套客户端源码。
- 新增：预留官网直装、Mac App Store 沙盒和 Microsoft Store MSIX 四类包装目标，
  当前仅供本地开发与验证，不开放下载、不启用自动更新、不提交应用商城。
- 安全：桌面端只加载安装包内的本地代码，启用 Chromium 沙盒、上下文隔离、
  最小 preload、CSP、权限默认拒绝和 Electron fuses，并限制导航与外部协议。
- 优化：Web 产物改用相对静态资源并移除远程字体依赖，确保同一构建可由 Web 与
  Electron 共同消费；生产构建不再误读本机 homeserver 覆盖配置。

## 2026-08-05 — Nexus OEM 购买与充值竖屏适配（不触发节点镜像）

- 修复：OEM“购买与充值”在手机竖屏下改为单列选择与全宽操作，
  企业转账表单不再被桌面最小宽度撑出屏幕。
- 优化：销售收益卡片在窄屏显示为两列，收益与提现记录保持局部横向滚动；
  七列订单表格增加独立滚动容器，不再扩大整个页面宽度。
- 交付：本次只修改集中托管的 Nexus 门户样式，不构建节点镜像，
  也不新增或改变节点更新任务。

## 2026-08-05 — GuDuu OS 1.22.4 (patch)

- 修复：所有 OEM 节点首次部署必须选择真实机房地域，兑换成功后立即进入
  Nexus 数据大屏的实例统计与地图定位，不再出现“已上报但地图无标点”。
- 修复：安装阶段暂时激活失败时，节点服务端后续重试会继续携带部署地域；
  历史漏定位节点必须补齐地域后才能重新激活。
- 优化：大屏显示已定位与待定位节点数，超管可在节点实例页纠正地域；
  已部署节点地域只能修改、不能清空，避免再次从地图静默消失。

## 2026-08-05 — Nexus 平台发布策略（不触发节点镜像）

- 新增：超管平台设置可配置开发、灰度和正式节点编号；初始规则固定为
  节点 1 开发、节点 2 公司技术灰度、节点 3 官网正式环境。
- 变更：CI 登记新的节点镜像摘要后自动只推节点 2，节点 2 未上报成功时，
  服务端拒绝给节点 3 建立正式安装任务。
- 安全：开发节点不接收灰度、正式发布或回撤任务；正式节点未部署或停用时，
  发布明确失败，不再出现“零台节点也算发布成功”。
- 交付：本次只修改集中托管的 Nexus 控制面，不生成新节点版本、不构建镜像，
  也不向节点 1、2、3 推送更新。

## 2026-08-05 — GuDuu OS 1.22.3 (patch)

- 新增：Nexus 超管加入独立“平台设置”页，统一管理 OEM
  层级数据和授权 Token 附赠两个功能开关。
- 变更：OEM 节点授权默认不附赠平台 Token，申请表和
  购买说明隐藏额度，API 接入方由 OEM 自由选择。
- 安全：开关关闭时，服务端在申请、超管审批和支付履约
  阶段均强制将新 KEY 的 Token 额度归零，防止绕过前端。

## 2026-08-05 — GuDuu OS 1.22.2 (patch)

- 新增：OEM 控制台“我的 KEY”为已审批、待部署的节点提供
  一键复制安装命令，官网部署中心同步说明授权、安装与激活流程。
- 安全：公开引导安装器只下载严格 Git tag 的发行工具，安装命令
  仅携带审批域名，长期 OEM KEY 仍由客户在 SSH 终端交互输入。
- 边界：节点发行版仍只包含 GuDuu OS bot/web 应用，Nexus OEM 后台
  继续作为平台独立项目部署，不进入客户节点镜像。

## 2026-08-04 — GuDuu OS 1.22.1 (patch)

- 发行：GHCR 同时提供 `vX.Y.Z` 与 `X.Y.Z` 两个不可变版本 Tag，
  便于人工拉取和灾备排障；OEM 自动安装与升级仍只使用冻结 digest。
- 修复：自建镜像仓登录或拉取失败时，宿主更新器会自动回退到
  相同 digest 的 GHCR 灾备镜像，不再因单仓故障阻断节点更新。

## 2026-08-04 — GuDuu OS 1.22.0 (minor)

- 修复：宿主更新代理使用固定 GuDuu OS 请求标识访问 Nexus，避免 Python urllib
  默认标识被 Cloudflare 误判为机器人，导致节点持续返回 403、无法领取镜像更新。
- 变更：网页发行版不再提供免授权本地部署，安装器强制要求已审批的 OEM KEY，
  并按审批域名从 Nexus 获取正式发布的 bot/web 不可变镜像摘要；首次安装不再
  在客户服务器现场构建源码，自建仓不可用时回退相同摘要的 GHCR 镜像。
- 新增：Nexus 增加部署中心；节点受限激活页可直接进入 OEM 登录、注册和授权
  申请，自动携带并预填当前部署域名，授权成功前继续阻断其他账号登录与注册。
- 修复：OEM 节点授权申请按“企业账号 + 规范化部署域名”阻止重复提交，
  并通过数据库行锁封住连续点击与并发请求；不同域名仍可分别购买节点，
  已拒绝或已撤回的域名允许修正资料后重新申请。
- 修复：Nexus 在 Cloudflare 之后使用 Caddy 2.6 项目专属头传递
  真实客户端 IP，严格 IP 绑定不再把 Cloudflare 边缘地址误当
  节点出口；非 Cloudflare 来源直连源站统一拒绝。
- 新增：节点镜像更新同时冻结 GHCR 与平台自建仓的精确摘要，
  优先从自建仓拉取，不可用时整组回退 GHCR。
- 新增：提供 Nexus 宿主 Registry 容器、独立数据盘挂载约定和
  systemd 同步任务，按原 digest 复制多架构镜像并重新校验内容。

## 2026-08-03 — GuDuu OS 1.21.2 (patch)

- 修复：Nexus 位于 Cloudflare 与 Caddy 反向代理之后时，不再把边缘节点地址
  当成访客 IP 提交给 Turnstile，避免安全验证成功后发码接口仍拒绝请求。
- 优化：人机验证、发码成功和失败结果会显示在当前验证码组件旁边，长表单
  无需滚回页面顶部即可看到明确状态。

## 2026-08-03 — GuDuu OS 1.21.1 (patch)

- 优化：Nexus 数据大屏的未授权、加载、空数据和故障状态页统一改为
  米白与橙色的控制台视觉，提升文字对比度与操作按钮辨识度。

## 2026-08-03 — GuDuu OS 1.21.0 (minor)

- 新增：Nexus OEM 注册、邮箱验证码登录和找回密码的发码入口接入独立
  Cloudflare Turnstile，降低机器人批量消耗企业邮箱额度的风险。
- 优化：服务端同时核验挑战结果、Nexus 门户域名和分用途 action；现有
  IP/邮箱限频继续生效，最终注册提交与普通密码登录不重复弹出验证。

## 2026-08-03 — GuDuu OS 1.20.1 (patch)

- 修复：SSH 单次恢复命令会安全读取 Nexus systemd 环境文件中的
  PostgreSQL 地址，避免普通 SSH shell 缺少服务环境时误写本地 SQLite。

## 2026-08-03 — GuDuu OS 1.20.0 (minor)

- 新增：Nexus 管理员灾难恢复改为服务器 SSH 生成的短期单次码，
  使用后立即作废，网页不再接受长期恢复凭据。
- 新增：CI 登记不可变 Docker 镜像清单后，版本中心自动生成默认
  未发布的节点版本草稿，更新说明从对应版本日志生成。
- 新增：AI 网关用量扣减加入持久化补偿队列，短暂数据库失败后
  会自动重试且不会重复扣费。
- 新增：Nexus 每小时维护任务清理过期会话、验证挑战、心跳和请求
  统计，同时重试未完成的 AI 用量结算。
- 修复：管理员、Passkey 和 OEM 登录会话改为先持久化再返回凭据，
  消除登录后立即访问偶发失效的竞态窗口。

## 2026-08-03 — GuDuu OS 1.19.1 (patch)

- 修复：镜像清单回调增加明确的 GitHub Actions 请求标识，避免被 Cloudflare
  将 Python 默认请求误判为机器人并在到达 Nexus 前拦截。
- 修复：节点体检正确识别 Cloudflare 代理域名，DNS 返回边缘 IP 时不再
  误报为源站解析故障并触发无效回撤。
- 优化：新镜像及回撤镜像启动后体检最多等待一分钟，避免 bot HTTP
  端口的短暂初始化窗口被误判为发布失败。

## 2026-08-02 — GuDuu OS 1.19.0 (minor)

- 新增：OEM 节点应用发布改为 GitHub Actions 构建 bot/web 多架构 Docker 镜像，
  Nexus 冻结并下发不可变 SHA-256 摘要，不再要求每台客户服务器日常现场编译源码。
- 新增：节点更新前自动备份 PostgreSQL 并保留当前运行镜像；新版本只切换 bot/web，
  Caddy 配置校验或完整体检失败时会自动恢复旧镜像并向 Nexus 上报失败。
- 优化：版本中心显示 Docker 构建清单与镜像摘要状态；严格 Git tag 更新保留为旧节点
  首次引导、更新器自身升级和故障救援通道，现有灰度、全量、暂停与历史回撤流程不变。
- 优化：GHCR 拉取凭据使用每台宿主机独立的 root 只读文件，Nexus 与 bot 容器均不保存
  仓库令牌或 Docker socket，客户域名、KEY、SMTP、证书和数据库不进入镜像。

## 2026-08-02 — GuDuu OS 1.18.0 (minor)

- 新增：OEM 节点授权申请必须填写计划部署域名，并可选填写静态公网 IP；
  管理端审批时把域名和 IP 策略冻结到对应 KEY，避免授权被转移到其他站点。
- 新增：节点在线激活会校验实际部署域名和由 Nexus 读取的真实来源 IP；域名不符
  必须拒绝，公网 IP 默认只产生风险告警，也可由申请人启用严格模式直接阻止激活。
- 优化：Nexus 不保存完整公网 IP，只保存不可逆 HMAC 指纹和脱敏网段；OEM 与超管
  列表同步显示绑定策略和风险状态，同域重装保持幂等，历史 KEY 首次激活自动建档。

## 2026-08-02 — GuDuu OS 1.17.5 (patch)

- 修复：平台 AI 对话改为只按模型实际输出 Token 结算，系统
  提示词、历史上下文、工具定义和缓存输入不再计入用户消费，
  避免一次普通咨询将 2,000 或 18,000 Token 余额异常清空。
- 优化：扣费流水新增 `model_output` 计费口径标记，管理后台同步明示
  “模型输出 Token × 倍率”，便于后续对账和解释；无需数据库迁移。

## 2026-08-02 — GuDuu OS 1.17.4 (patch)

- 优化：Token 经济中的汇率 Token 数、每日免费量、新用户赠送量、充值包
  Token 数和人工调整量统一限制为整数，失焦及保存时都会自动规整。
- 优化：充值包人民币金额在输入阶段最多保留两位小数；计费倍率最多保留
  两位且必须大于 0，非法值回到安全默认值 1。
- 修复：服务端同步校验倍率精度和正数边界，避免绕过前端写入 0 或浮点长尾，
  造成管理后台展示值与实际扣费值不一致；无需数据库迁移。

## 2026-08-02 — GuDuu OS 1.17.3 (patch)

- 修复：创作者 Agent 的审核标价不再受平台内置 AI 用量开关影响；除创作者
  本人外，普通用户和平台管理员每次使用都按标价扣 Token 并给创作者分成。
- 修复：创作者付费 Agent 的计费前置检查异常时改为阻断调用，不再因钱包服务
  故障静默放行一条免费回复；创作者技能买断采用同一商品定价边界。
- 优化：商城按钮由“免费获取”改为“获取（使用时扣费）”，获取前明确提示按次
  价格；钱包和后台流水补充创作者 Agent、技能购买与创作收益类型名称。

## 2026-08-02 — GuDuu OS 1.17.2 (patch)

- 修复：管理后台手动调整 Token 前会校验完整账号域和 Synapse 真实用户，
  不再允许输错 Matrix ID 后创建“后台有余额、真实用户为 0”的孤立钱包。
- 修复：用户余额、收支明细和后台余额查询统一禁止浏览器缓存，管理员跨账号
  入账后重新打开页面即可读取最新余额与流水。
- 优化：后台不再吞掉用户不存在、账号域错误和临时核验失败，直接显示可操作的
  中文原因；本次不依赖支付渠道，也无需数据库迁移。

## 2026-08-02 — GuDuu OS 1.17.1 (patch)

- 修复：管理员手动增减 Token 后，流水时间不再比电脑系统时间少 8 小时；
  服务端明确返回 UTC 时区，管理后台与用户充值明细按当前设备时区显示。
- 优化：兼容历史接口返回的无时区流水时间，不修改既有账本数据，也无需数据库迁移。

## 2026-08-02 — GuDuu OS 1.17.0 (minor)

- 新增：任务看板增加“待审核”列；每条新任务指定一位真人审核人，
  默认选频道管理员或任务下达人，卡片上直接显示审核责任人。
- 变更：执行人、AI Agent 和工作流只能把交付提交为待审；只有指定
  真人审核人才能审核通过为已完成，审核前不会计入项目完成或触发归档。
- 修复：非频道真人成员、主 AI 或协作 AI 账号不再能被当成审核人；
  无效候选人会安全回退到频道管理员/下达人。
- 变更：版本规则从本版起区分功能与修复；`feature` 升 MINOR，
  `bugfix/maintenance` 升 PATCH，并新增跨会话发布台账防止版本记忆冲突。

## 2026-08-02 — GuDuu OS 1.16.5 (patch)

- 新增：Nexus OEM 企业账号注册必须验证邮箱，门户加入六位邮箱验证码发送与十分钟
  有效期提示；验证码原文不落库，不同用途不能混用。
- 新增：OEM 门户支持邮箱验证码登录和找回密码；重置成功后自动撤销该企业账号的
  全部旧会话，降低旧设备或泄露会话继续访问的风险。
- 安全：验证码加入单邮箱冷却、小时发送上限、输错锁定和 IP 级接口限频；不存在或
  停用账号的登录/找回请求使用统一响应，减少账号枚举风险。
- 修复：下级 OEM 邀请链接会稳定打开注册表单，不再在页面初始化时跳回密码登录。
- 变更：本次只更新中央 Nexus；现有 OEM 账号仍可使用密码登录，OEM 节点无需升级。

## 2026-08-02 — GuDuu OS 1.16.4 (patch)

- 变更：项目规范、架构、待办、部署记录和现役源码不再保留退役云环境、旧服务器
  IP 与旧域名；生产基础设施统一以当前 Google Cloud 和 `guduu.co` 域名体系为准。
- 修复：主客户端默认 Matrix API、homeserver 白名单与发行版默认 Nexus 地址改为当前
  服务地址，避免新浏览器、本地预览或新装 OEM 节点误连已停用服务。
- 优化：Matrix ID 示例统一使用保留测试域名 `example.invalid`，支持邮箱改为
  `support@guduu.co`；云元数据 SSRF 防护保留，但去除对退役供应商的部署描述。
- 变更：`TODO.md` 重建为当前唯一未完成清单，`CLAUDE.md` 收敛为指向
  `AGENTS.md` 的兼容入口，避免重复项目规范再次漂移。

## 2026-08-02 — GuDuu OS 1.16.3 (patch)

- 修复：中枢 AI 在私人会话拆分任务时，不再允许直接把任务派给其他真人；
  这类操作现在会在落库前整批拒绝，避免生成没有真实共享频道的“假专班”和
  不可见悬空任务。
- 优化：私人会话需要多人分工时，强制中枢 AI 改走 `assemble_team`，一次完成
  建频道、拉真人和派单；如果建专班被会员权限拒绝，必须如实回报，不再用
  `create_tasks` 兜底产生孤立任务。

## 2026-08-02 — GuDuu OS 1.16.2 (patch)

- 新增：超管控制台加入独立“使用教程”，集中说明 Cloudflare Access、MFA、
  具名管理员、Passkey、第二管理员开通、应急恢复与离职撤权流程。
- 新增：教程页面右上角支持一键复制完整 Markdown；网页与复制内容由同一份
  结构化数据生成，后续更新不会出现两份文档不一致。
- 安全：教程不写入服务器地址、令牌、密码、动态验证码等部署秘密，并明确要求
  每位管理员使用独立邮箱、Nexus 账号和 MFA/Passkey。

## 2026-08-02 — GuDuu OS 1.16.1 (patch)

- 修复：中枢 AI 拆分或改派任务给频道外真人时，不再只发出待接受邀请；
  服务端现在会将本地账号立即加入任务所属频道，确保 AI 查询频道真人成员
  能看到该用户，用户登录后侧栏也能看到频道与任务。
- 优化：自动加入因管理配置或服务异常失败时，系统保留标准邀请，并明确
  回报“仍待接受”及真实错误，不再误报已入频道。

## 2026-08-01 — GuDuu OS 1.16.0 (minor)

- 新增：平台超级管理员支持 WebAuthn / Passkey，可使用 Touch ID、安全密钥或
  已同步的 Passkey 登录；密码保留为备用方式，凭据可在“平台安全”中登记和移除。
- 新增：Passkey 注册与登录使用五分钟一次性挑战、强制用户验证和服务端固定的
  RP ID / Origin；设备私钥永不上传，数据库只保存公开凭据与签名计数器。
- 安全：新增独立管理域名边界；正式切换后，超管页面、会话 Cookie 和所有
  `/nexus/admin/*` API 仅接受管理主机请求，不能从 OEM 域名绕过 Cloudflare Access。
- 优化：独立管理站中的数据大屏入口会回到公开 Nexus 域名，继续只兑换只读大屏
  令牌，不扩大管理会话权限。
- 变更：本版采用分阶段启用；部署代码不会立即切断旧入口，只有配置管理域名和
  WebAuthn 环境变量后才执行域名封锁，便于先完成 Cloudflare Access 和真实设备登记。

## 2026-08-01 — GuDuu OS 1.15.1 (patch)

- 新增：平台超级管理员改用独立登录地址 `/portal/admin/`，支持刷新、收藏和直接
  访问；登录后超管工作台始终回到该地址。
- 优化：普通 `/portal/` 只展示 OEM 登录和注册，不再提供超管切换入口；OEM 与
  超管已有会话进入错误入口时会自动回到各自控制台。
- 安全：独立超管入口禁止搜索引擎收录，并继续执行严格 CSP 与服务端逐请求鉴权；
  独立 URL 仅用于入口隔离，不替代具名账号、短会话和权限校验。
- 安全：管理员会话从浏览器 `sessionStorage` 迁移到不可被脚本读取的
  HttpOnly、Secure、SameSite=Strict 非持久化 Cookie，管理写操作增加 CSRF 请求头；
  应急静态令牌提交后只换取 30 分钟恢复 Cookie，不再保存或返回令牌原文。

## 2026-08-01 — GuDuu OS 1.15.0 (minor)

- 新增：Nexus 超管由共享服务器令牌升级为具名管理员账号，使用短期、可撤销的
  12 小时登录会话，并支持创建、启停和重置管理员密码。
- 新增：超管左侧加入“平台安全”，集中展示当前登录身份、管理员清单和全局审计
  记录，可按操作人及对象类型追查管理动作。
- 安全：停用账号或重置密码会立即撤销其全部会话；禁止自停用和停用最后一个
  有效管理员，避免平台被误锁死。
- 优化：原服务器令牌保留为首次创建管理员及故障恢复入口；Portal 启用严格 CSP，
  页面脚本改为同源外链，降低脚本注入风险。
- 变更：本次仅更新中央 Nexus，无 OEM 节点数据迁移；现有 OEM 账号、KEY、订单、
  收益、层级和版本记录均保持不变。

## 2026-08-01 — GuDuu OS 1.14.3 (patch)

- 优化：OEM“我的 KEY”改为授权卡片与汇总视图，展示真实节点域名、初始
  Token、当前余额、版本、签发和兑换时间，不再用实例编号代替业务信息。
- 修复：授权有效、节点绑定和节点心跳拆分为三套独立状态，KEY 不再错误显示
  为“在线”；暂停或吊销的未绑定 KEY 明确提示当前不可部署。
- 新增：已绑定 KEY 可直接打开对应节点详情，或进入 Token 充值并自动选中
  目标节点；线下授权码认领入口保留并降低视觉层级。
- 安全：授权列表继续只显示 KEY 尾号，完整 KEY 仅在签发或获批领取窗口
  一次性展示，避免控制台长期暴露授权凭据。

## 2026-08-01 — GuDuu OS 1.14.2 (patch)

- 修复：OEM“授权申请”表单改为稳定的十二列网格，部署域名、使用场景、
  日期和付款方式按业务顺序等宽排列，不再因字段内容或隐藏项发生错位。
- 优化：企业转账资料保持三项等宽，在线渠道与人工 Token 字段出现时独占
  完整一行；补充说明和提交按钮底边对齐，平板及手机自动降为两列和单列。

## 2026-08-01 — GuDuu OS 1.14.1 (patch)

- 修复：OEM“申请提现”表单不再复用容易错位的横向流式布局，金额、收款方式、
  收款人、账号、开户行和备注改为按业务顺序排列的独立网格。
- 优化：桌面端统一字段宽度和输入基线，提交按钮与备注框底边对齐；平板端改为
  两列、手机端改为单列，避免窄屏拥挤或产生横向滚动。

## 2026-08-01 — GuDuu OS 1.14.0 (minor)

- 新增：OEM“购买与充值”形成统一销售闭环，可购买下级 OEM 授权兑换码，
  也可为自己的节点购买 Token 充值包；在线渠道与企业转账统一由平台收款。
- 新增：客户付款成功后，系统只把“客户实付－直属 OEM 结算成本”的价差
  记入直属 OEM 收益，不做跨层返佣；平台直属客户不产生 OEM 收益。
- 新增：超管可设置平台主管结算价，也可给每个 OEM 单独设置授权结算价和
  Token 结算比例；下单时冻结价格与归属，之后改价不会修改历史收益。
- 新增：OEM 门户展示待结算、可提现、累计收益、价差流水和提现历史，支持
  银行卡、支付宝、微信与 PayPal 收款资料；敏感账号只加密保存并脱敏回显。
- 新增：超管加入提现审核工作台，支持批准、填写线下打款流水并确认已打款，
  或驳回后自动退回余额；收益与提现使用只追加账本保障幂等和可审计。
- 优化：Token 企业转账强制上传付款凭证，只有超管核实到账后才充值并记收益；
  平台营收剔除关联转账行，避免订单与企业转账重复统计。

## 2026-08-01 — GuDuu OS 1.13.6 (patch)

- 修复：OEM 网络数据开关关闭时不再隐藏整个邀请页面，普通用户和下级 OEM
  的分享链接、分享码及二维码始终保留可用。
- 变更：开关范围收窄为层级统计、下属 OEM 清单和归属用户清单；关闭时
  服务端只裁掉这些数据并阻断目录接口，不影响新归属关系继续记录。

## 2026-08-01 — GuDuu OS 1.13.5 (patch)

- 新增：平台超管“舰队总览”加入 OEM 层级与用户数据开关，配置持久化保存，
  可随时控制所有 OEM 是否能使用邀请与层级功能。
- 变更：开关默认关闭；关闭时 OEM 左侧不显示“邀请与层级”，首页不返回
  层级统计，且下属清单与分享二维码接口同步拒绝访问，不能绕过前端读取。
- 优化：关闭功能不会删除既有企业层级和用户归属关系，重新开启后原有数据
  与分享能力立即恢复，平台超管仍可正常管理全部真实数据。

## 2026-08-01 — GuDuu OS 1.13.4 (patch)

- 优化：OEM“我的首页”节点详情改为在实例卡片下方直接展开，不再使用
  遮罩弹窗，浏览节点数据时可同时保留首页上下文。
- 优化：实例卡显示当前选中状态，支持再次点击或使用“收起详情”关闭；
  刷新首页时会保留选中节点并重新读取最新真实心跳数据。
- 优化：桌面端和手机端分别使用三列、两列运营指标布局，390px 手机
  宽度无横向溢出；超管节点详情弹窗保持原样。

## 2026-08-01 — GuDuu OS 1.13.3 (patch)

- 新增：OEM“我的实例”卡片可直接点开节点详情，查看运行状态、
  版本、地域、Token 余额/消耗、AI 请求和开通时长。
- 新增：节点运营快照展示真实账号分类、房间/频道、会员、知识库、
  Skill、AI Agent 和工作流数据，与超管节点详情保持同一口径。
- 优化：OEM 节点详情在服务端根据 KEY 认领关系强制隔离，不得查看
  其他企业节点；桌面和手机弹窗均支持完整滚动查看。

## 2026-08-01 — GuDuu OS 1.13.2 (patch)

- 新增：OEM“邀请与层级”页加入真实下属清单，可查看直属与
  间接下级企业、直属上级、层级、用户数、下级数与注册时间。
- 新增：归属用户清单展示 Matrix 用户 ID、直属企业、注册实例、
  直属/间接关系与归属时间，无数据时给出明确引导。
- 优化：下属目录由服务端按当前 OEM 的整棵后代树强制隔离，不返回
  下属企业联系信息，也不会混入上级、旁支或其他 OEM 数据。

## 2026-08-01 — GuDuu OS 1.13.1 (patch)

- 新增：OEM 企业控制台加入左侧功能导航，将实例、邀请层级、版本公告、
  购买充值、授权申请和 KEY 管理拆分为六个独立页面。
- 新增：OEM 功能页使用 `#oem-*` 地址保存当前位置，支持浏览器前进、后退、
  刷新和深链；导航角标显示该企业自己的实例、公告、订单、申请与 KEY 数量。
- 优化：购买授权入口会自动跳转到授权申请并保留所选支付渠道；窄屏下左侧栏
  自动转换为顶部横向导航，390px 手机宽度无页面横向溢出。

## 2026-08-01 — GuDuu OS 1.13.0 (minor)

- 新增：OEM 门户把节点授权申请升级为统一购买流程，可选择在线支付、
  企业转账或合同/免费授权；一张申请只关联一笔付款和一把 KEY。
- 新增：在线订单由服务端锁定价格与 Token，支付回调验签并核对金额、币种后
  自动签发；重复回调保持幂等，新订单不再通过订单接口返回 KEY 明文。
- 新增：OEM 企业转账必须填写付款企业并上传图片凭证，超管可在授权中心核对
  真实到账后一次完成营收确认、KEY 签发和加密交付，也可要求补资料或拒绝。
- 优化：待核对转账只计入待支付金额，不计入累计营收；到账确认后自动转入
  企业转账实收，OEM 可在七天内从授权申请主动领取 KEY。
- 优化：合同或免费授权保留原有人工审批；所有付款和审批路径共用审计、归属、
  加密交付及并发保护，凭证或 AI 识别结果本身不能触发发码。

## 2026-08-01 — GuDuu OS 1.12.0 (minor)

- 新增：Nexus“授权与申请”升级为完整授权中心，OEM 可填写企业、
  计划域名、使用场景、期望日期、Token 需求和联系资料，并查看全部历史。
- 新增：申请支持修改、补资料重提和主动撤回；超管可按状态筛选、
  搜索、查看详情与完整审计时间线，并选择批准、要求补充或拒绝。
- 新增：授权 KEY 支持可恢复的暂停/恢复和不可逆的永久吊销，
  所有管理操作强制填写原因并留存审计记录；列表直接显示企业归属。
- 修复：批准申请改为数据库行锁与状态校验，避免连点或并发审批时
  重复签发 KEY。
- 优化：申请批准后的 KEY 明文不再返回给超管或出现在历史列表，
  而是用 Nexus 主密钥加密保存，仅允许申请企业在限时窗口中主动领取。

## 2026-08-01 — GuDuu OS 1.11.8 (patch)

- 优化：Nexus 舰队总览重构为核心运行状态、节点与用户、内容与 AI 资产、
  AI 用量四层信息结构，解决大量同等级数据卡片难以浏览的问题。
- 优化：核心指标使用更清晰的视觉层级与在线状态提示，详细指标改为紧凑分组；
  浅色、暗色及窄屏保持自适应，原有真实数据口径不变。

## 2026-08-01 — GuDuu OS 1.11.7 (patch)

- 新增：Nexus 舰队总览接入各节点真实的业务频道、知识库、技能、
  AI Agent、工作流定义与累计运行数据，并显示实际上报节点覆盖率。
- 新增：节点详情补充空间、AI 会话房、私聊房、知识库文档与分块、
  自建技能/Agent 总数及启用数，便于逐节点核对内容与 AI 能力资产。
- 优化：业务频道沿用后台真实房型标记，排除空间、私聊和 AI 会话；
  数据源读取失败时缺省指标而不是填入演示数或伪造的 0。

## 2026-08-01 — GuDuu OS 1.11.6 (patch)

- 新增：Nexus 节点运营快照加入真实聊天房间数、已开通天数、
  有效/付费/创作者会员、工作流运行、已支付订单和知识库文档数。
- 新增：节点列表接口从 Nexus 账本聚合累计/今日 Token 消耗与
  AI 请求次数，舰队总览与节点详情都可直接查看。
- 优化：详情弹窗扩宽并标注数据口径；“今日消息”明确为 Bot 当日
  进程计数，不冒充可持久对账的历史消息总数。

## 2026-08-01 — GuDuu OS 1.11.5 (patch)

- 新增：OEM 节点心跳将 Synapse 本地账号拆分为业务用户、
  管理员、AI/系统账号、访客、停用和锁定账号，各分项可与账号总数对账。
- 新增：Nexus 节点列表直接展示业务用户、管理员与 AI 数量，
  详情弹窗展示全部人员口径，避免把 Nexus 账号或 AI 误当普通用户。
- 优化：大屏的用户指标对新节点改为业务用户口径，同时保留账号总数、
  管理员和 AI 汇总数据；未升级节点继续兼容旧数据。

## 2026-08-01 — GuDuu OS 1.11.4 (patch)

- 新增：Nexus 节点实例列表加入详情入口，可查看企业归属、域名、管理员、
  在线状态、版本、心跳、地域、来源网段、Token 余额和心跳统计。
- 新增：已绑定 OEM 的节点可从详情弹窗直接进入对应企业档案；历史未绑定
  节点保持只读运行信息，不显示错误的企业跳转。

## 2026-08-01 — GuDuu OS 1.11.3 (patch)

- 新增：Nexus “节点实例”列表增加 OEM 企业名与账号邮箱，企业信息根据
  KEY 的真实认领关系自动关联，无需重复维护实例企业字段。
- 优化：早期尚未绑定 OEM 的历史节点明确显示“未绑定企业”，不根据
  域名或管理员邮箱猜测归属。

## 2026-08-01 — GuDuu OS 1.11.2 (patch)

- 优化：Nexus 版本中心的“更新说明”编辑区默认展开，自动生成的多条公告
  可直接完整审阅；桌面端提供更大高度，移动端保留适中高度和竖向拖拽。

## 2026-08-01 — GuDuu OS 1.11.1 (patch)

- 新增：Nexus 版本中心加入明确的更新对象选择，将“Nexus 平台”和“OEM 节点”拆成
  两条互不干扰的发布轨道；既有历史版本继续按节点版本兼容展示。
- 新增：Nexus 平台更新由我们集中部署，发布后直接向全部 OEM 后台展示公告，即使
  客户暂时没有节点也能收到；不会创建客户服务器安装任务或提供节点灰度、回撤操作。
- 优化：OEM 节点更新继续使用 Git tag、灰度、全量、暂停、失败重试和历史回撤，只有
  所属节点上报安装成功后才向对应 OEM 展示节点公告，不泄露其他客户节点信息。
- 优化：版本表单默认选择风险更低的 Nexus 平台轨道，并动态隐藏灰度节点；历史卡片、
  确认提示和 OEM 公告均明确标注平台或节点，避免超管误把后台公告当成服务器升级。

## 2026-08-01 — GuDuu OS 1.11.0 (minor)

- 新增：Nexus 舰队总览在微信支付下加入独立企业转账入口，支持手动填写人民币金额、
  关联 OEM、企业/银行/账户/流水信息，并强制上传真实 JPG、PNG 或 WebP 转账凭证。
- 新增：凭证可由视觉 AI 识别金额、付款/收款企业、账户、银行、流水号和转账时间后
  自动预填；图片只在超管主动点击时发送，AI 结果不自动入账，仍须人工核实到账。
- 新增：企业转账生成独立 `BT` 单号，计入累计与近 30 日实收；舰队统计、收入构成和
  订单列表以紫色单独展示金额和笔数，并允许超管鉴权查看原始凭证。
- 优化：银行详情、AI 候选结果和凭证原图均使用 Nexus 主密钥加密写入 PostgreSQL，
  同一凭证禁止重复登记；测试 Stripe 凭据改为运行时拼接，避免 Secret Scanning 误报。
- 变更：舰队资金概览右上角重复的“点击渠道配置 API”提示已移除；各渠道自己的配置
  入口保持不变。未配置视觉模型密钥时，企业转账继续支持完整的人工登记流程。

## 2026-08-01 — GuDuu OS 1.10.0 (minor)

- 新增：Nexus 舰队总览的支付宝、微信支付、Stripe、PayPal 与 USDT 渠道均可点击
  打开 API 配置，按官方要求展示字段、获取说明、官方文档与自动生成的回调地址。
- 新增：支付凭据由 Nexus 独立主密钥加密后存入 PostgreSQL；前端与管理 API 只显示
  字段是否已配置，空白更新保留旧值，绝不回填、记录或返回密钥原文。
- 新增：Stripe、PayPal 与 NOWPayments 保存时执行官方无扣款认证检查；支付宝和微信
  支付执行 RSA/APIv3 本地格式检查，并明确提示仍需沙箱交易与回调验签。
- 优化：控制台区分“未配置、验证失败、本地格式通过、官方认证通过、真实交易开放”，
  防止把仅填过 Key 的渠道误认为已经可以收款；本次新增数据库表，无需手工迁移。

## 2026-08-01 — GuDuu OS 1.9.1 (patch)

- 新增：Nexus 舰队总览把支付渠道拆分为国内与海外两区，海外区加入 Stripe、
  PayPal 和 USDT 的接入状态、适用支付方式与币种提示。
- 新增：Nexus 支付抽象层登记三个海外渠道的凭据状态，未配置或未联调时一律拒绝
  真实下单并显示“待接 API”，不会生成虚假交易。
- 优化：海外支付明确按原币分别核算；正式收款前必须补齐订单币种、原币金额与结算
  金额，避免将 USD 或 USDT 错误计入人民币收入。

## 2026-08-01 — GuDuu OS 1.9.0 (minor)

- 新增：每个 OEM 自动获得稳定分享码，可在门户复制普通用户注册链接、下级 OEM
  邀请链接，并直接分享由 Nexus 本机生成的二维码。
- 新增：普通用户通过 OEM 链接注册时，注册页会显示邀请方；建号成功后先在实例本地
  持久化归属，再通过实例授权 KEY 幂等同步 Nexus，母舰临时断网也不会丢关系。
- 新增：OEM 采用单直属上级、任意深度的层级树，不限制层数；超级管理员可以查看每家
  OEM 的层级、直属/全部下级数量，以及直属/全网络用户数量。
- 优化：分享码、实例 KEY、OEM 归属和 Matrix 用户域名必须一致后才允许登记；Nexus
  只保存用户 ID 与归属边，不保存用户密码、验证码、聊天数据，也不引入分账或佣金。

## 2026-08-01 — GuDuu OS 1.8.0 (minor)

- 新增：Nexus 舰队总览加入资金与经营概览，展示累计已支付、近 30 天已支付、
  待支付金额和订单数量。
- 新增：收入按授权码买断与 Token 在线充值拆分，并展示全舰队 Token 余额与
  已交付充值额度。
- 优化：只有服务端确认 `paid` 的订单计入收入，待支付订单不作为营收；汇总覆盖
  全部历史订单，不受最近 200 单列表限制。
- 变更：支付宝与微信支付 API 尚未接入时明确显示“待接 API”；拿到凭据并完成
  回调验签后，现有资金总览会自动使用真实数据，无需重做页面。

## 2026-08-01 — GuDuu OS 1.7.4 (patch)

- 修复：当前产品版本已经保存到历史列表后，版本管理表单仍会显示自动生成的版本号、
  Git tag、标题和完整更新说明，不再只留下空白占位文字。
- 优化：已保存版本的按钮会明确显示“当前版本已保存”并禁止重复提交；管理员仍可查看、
  选择和复制公告文字，也可手动输入其他版本号进行应急录入。

## 2026-07-31 — GuDuu OS 1.7.3 (patch)

- 新增：Nexus 版本发布表单会读取当前产品版本与 `DEVLOG.md`，自动生成版本号、Git
  tag、版本标题和面向 OEM 的更新说明，超管审阅后再保存为未发布。
- 新增：OEM 节点完成版本安装并成功回报后，同一份更新说明会自动出现在所属 OEM
  门户的“版本更新公告”中，无需重复维护公告内容。
- 修复：版本公告严格按照授权码归属过滤，未安装成功的版本不提前展示，不同 OEM
  之间不能看到对方的节点域名或公告记录。
- 优化：公告由版本记录与成功投放结果实时组合，不新增重复数据，也不会向客户业务群
  自动发送消息；本次更新无需数据库迁移。

## 2026-07-31 — GuDuu OS 1.7.2 (patch)

- 新增：Nexus 超级管理员控制台加入左侧功能导航，将舰队总览、版本管理、节点实例、
  授权与申请、OEM 客户、支付与订单、数据大屏拆成清晰入口。
- 新增：导航实时显示版本、节点、待处理申请、客户和订单数量，管理人员无需逐页进入
  即可看到各功能区规模。
- 优化：当前功能页写入地址栏 Hash，支持刷新保留、深链、浏览器前进和后退；移动端
  自动收为顶部横向滑动菜单，不挤压管理表单。
- 优化：每个功能页提供统一标题与“刷新当前数据”操作，后台不再把所有模块纵向堆在
  同一个超长页面中。

## 2026-07-31 — GuDuu OS 1.7.1 (patch)

- 修复：超级管理员从登录页进入后台后，数据大屏只读令牌没有初始化，导致已经登录
  仍无法打开大屏；现在登录成功即自动兑换只读凭据，并在总控首页提供明确入口。
- 优化：新建版本在后台统一显示为“未发布”，按钮和流程说明不再混用“草稿”措辞；
  历史版本按最新在前永久保留。
- 新增：历史列表可选择曾经全量发布过的旧版本发起全节点回撤，并实时监测各节点结果；
  未发布或仅灰度版本不能作为回撤目标，执行前必须二次确认。
- 变更：发行版更新脚本改为精确检出不可变 `vX.Y.Z` tag，同时支持向新版本升级和向
  历史版本回撤；实例业务数据与数据库卷不随代码版本切换。

## 2026-07-31 — GuDuu OS 1.7.0 (minor)

- 新增：GuDuu Nexus 超级管理员后台加入“版本发布中心”，可维护版本号、Git tag、
  更新标题与说明，并按“草稿→灰度监测→全量发布→暂停”管理发行生命周期。
- 新增：版本发布可查看每个 OEM 节点的待领取、下载中、安装中、成功与失败状态；
  失败节点不会无限自动重试，须由超级管理员明确安排重试。
- 新增：OEM 发行版加入宿主机自动更新代理，每 5 分钟通过现有授权 KEY 向 Nexus
  检查一次；只接受严格的 `vX.Y.Z` tag，不开放入站端口，也不向平台交付 SSH 凭据。
- 优化：发行版升级脚本支持锁定已发布 tag，并在发现本地源码改动时停止自动升级，
  避免覆盖 OEM 定制；更新结果会回传 Nexus 供后台监测。
- 变更：1.7.0 需要对既有节点人工安装一次更新代理；从后续版本开始即可由 Nexus
  版本发布中心统一灰度和全量推送。


---

更早的开发流水已按现役资料净化规则移除；当前架构、部署状态和待办分别以 `AGENTS.md`、`ARCHITECTURE.md`、本机 `DEPLOY.md` 与 `TODO.md` 为准。

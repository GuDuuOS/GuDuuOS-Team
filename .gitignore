# 上游 Synapse 源码（独立仓库，不纳入 GuDuu 仓库）
synapse/

# 部署运维记录（含服务器拓扑，本地保留、不托管）
DEPLOY.md

# 本地专用密钥/连接文件 —— 绝不上传 GitHub、绝不放服务器
CREDENTIALS.local.md
*.local.md
*.local
secrets*

# 本地运行产物，不提交
.venv/
.venv312/
run/

# Python 缓存
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# 临时/日志
*.log

# 杂项 / 系统文件 / 打包产物
.DS_Store
.claude/
*.zip

# AI 生成的原始大图素材（巨大、未被网页引用，仅本地保留）
hf_*.png

# 演示稿/设计参考（仅本地参考，绝不上传 GitHub）
GuDuu-筱雨工作室/
*演示稿*/
design-ref/

# Guduu DI 独立产品源码(单独部署,不属于 CosMac 仓库)
guduu-di-online/

# 对外介绍等 office 文档是负责人工作文件,不入代码仓库(本地保留)
*.docx

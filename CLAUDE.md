# GuDuu OS — 兼容入口

本仓库只维护一份项目规范：[`AGENTS.md`](AGENTS.md)。

任何开发工具或 AI 在开始工作前都必须完整读取 `AGENTS.md`，并以其中的架构原则、路线图、
开发约定、版本规则和生产部署规则为准。本文件不再复制项目规范，避免两份规则长期漂移。

生产环境只使用当前 Google Cloud 架构。现役主机、域名、账号和目录仅记录在本机被忽略的
`DEPLOY.md`；不得从历史提交、旧文档或旧环境推断生产目标，也不得把凭据、IP 或数据库连接
写入版本库。

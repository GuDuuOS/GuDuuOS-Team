# GuDuu OS 私有 GHCR 只读凭据。
# 正式节点请在宿主机直接填写，不要把真实值发到聊天、Nexus 或提交到 Git。
# GitHub classic PAT 只授予 read:packages；如果镜像包设为 public，可保持空值。
GUDUU_REGISTRY_USER=
GUDUU_REGISTRY_TOKEN=

# 平台自建镜像仓的节点专用只读账号，应可随时撤销。
GUDUU_MIRROR_USER=
GUDUU_MIRROR_TOKEN=
